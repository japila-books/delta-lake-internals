# UCCommitCoordinatorClient

`UCCommitCoordinatorClient` is a [CommitCoordinatorClient](CommitCoordinatorClient.md) that uses [Unity Catalog]({{ book.unity_catalog }}) as the commit coordinator.

## Creating Instance

`UCCommitCoordinatorClient` takes the following to be created:

* <span id="conf"> Map<String, String>
* <span id="ucClient"> [UCClient](UCClient.md)

`UCCommitCoordinatorClient` is created when:

* FIXME
